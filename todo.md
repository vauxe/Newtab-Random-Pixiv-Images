# TODO

仅针对 `src` 目录整理，暂不考虑 `src_firefox`。

## 功能待办

- [ ] 完成当前页面图片的收藏功能
  说明：前端按钮目前只打开作品页，未真正调用后台收藏逻辑，需要补齐前后台联动、状态反馈和失败提示。

- [ ] 优化随机选择的随机性
  说明：当前实现是随机页 + 随机作品 + 最近看过去重，仍可能重复偏多，需要改进抽样策略和短期去重机制。

- [ ] 支持按用户维度进行喜欢 / 不喜欢
  说明：当前只支持基于 tag 进行排除，后续需要支持对作者 / 用户进行喜欢、屏蔽或降权。

- [ ] 支持随机 tag 池
  说明：请求时采用“强限制条件 + 随机抽取部分 tag 池”的方式构造查询，避免每次请求 URL 都拼接大量 tag。

- [ ] 首次打开时支持默认图
  说明：当前初始加载依赖实时请求 Pixiv，响应较慢；需要在首屏阶段支持展示默认图或本地兜底图。

- [ ] 支持默认图片配置
  说明：默认图不能写死，需要支持在配置页中设置默认图来源、预览当前默认图；远程 URL 走 `storage.local`，本地上传图片走 `IndexedDB`。

- [ ] 支持一键开关随机图片功能
  说明：关闭随机图片后，新标签页固定显示当前配置的默认图；开启后恢复正常随机拉取。
